---
"@cosyte/synth": patch
---

The README now opens with the Cosyte brand lockup, which follows the reader's light or dark theme, and it no longer says the package is unpublished when it is on npm.

The image is served as a `<picture>` with a light and a dark source, and carries alt text describing the mark so it still reads for anyone with images off or a screen reader on.

The page also said, at the top of its summary, that the package was "not yet published to npm", directly under the version npm renders on the same page. It now says the package is published and points at the npm package page for the version that is live, so the registry stays the single source of truth and this page cannot drift from it.

Nothing else moved: the title, the code samples and every documented generator, subpath and option are unchanged, and no generated byte differs.
