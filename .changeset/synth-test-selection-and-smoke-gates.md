---
"@cosyte/synth": patch
---

No change to the published package: no export, no signature and no generated byte differs.

Two repository CI checks were added, so a pull request now has to prove what it previously only
assumed: that the test suites this package's synthetic-data guarantees rest on are the ones CI
actually selects, and that every published subpath still loads and generates under both ESM and CJS.
