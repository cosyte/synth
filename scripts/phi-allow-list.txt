# scripts/phi-allow-list.txt
#
# PHI-scan allow-list: the positive declaration that a fixture's identifiers
# are synthetic. `scripts/phi-scan.ts` reads this file; any realistic-PHI-shaped
# token NOT covered here is a hit. Byte-strict wire formats cannot carry an
# inline `# synthetic: true` header (the header segment must be the first bytes),
# so this allow-list is the synthetic-fixture declaration mechanism, the same
# approach every sibling `@cosyte/*` parser uses.
#
# Format: `<TAG> <value>` per line. `#` comments and blank lines are ignored.
#   NAME <token>          synthetic person-name word, matched case-insensitively
#   DOB <value>           synthetic date of birth (normalize as your detector does)
#   ID <value>            synthetic id matching an SSN / MRN / member-id shape
#   EMAILDOMAIN <domain>  reserved / test email domain that is allowed to appear
#   EMAIL <address>       one exact address, allowed wherever it appears
#
# WHICH TAGS THE SCANNER ACTUALLY CONSUMES. NAME feeds every structured name
# detector (HL7 PID-5, FHIR HumanName, C-CDA recordTarget, X12 NM1/PER, NCPDP
# SCRIPT + Telecom, ASTM P-6). EMAILDOMAIN and EMAIL feed the email floor. DOB and
# ID are loaded but consumed by nothing: no arm value-gates a DOB (a seeded
# synthetic DOB is indistinguishable from a real one, and there is no reserved DOB
# range), and the id arms check a SHAPE, SSA never-issued range, synthetic
# assigning authority, a failing NPI Luhn or DEA checksum, rather than a declared
# literal, because a generator mints ids per seed and a literal list would never
# keep up. Both tags are kept so a future rule has a declared source to read.
#
# To add a new synthetic fixture: reuse the tokens below, or append new ones here
# and commit. Adding a token is a reviewed, intentional act, never silent.

# --- Reserved / test email domains (RFC 2606 / 6761) ---
EMAILDOMAIN example.com
EMAILDOMAIN example.org
EMAILDOMAIN example.net
EMAILDOMAIN example.test

# --- Exact addresses cleared one at a time ---
#
# The scan covers all of `test/`, which includes `test/us-core-profiles/`: the
# real, HL7-published US Core 6.1.0 StructureDefinitions this repo vendors so US
# Core conformance is validated against the normative artifact rather than a
# paraphrase. Each one carries its publisher's work-group contact in
# `contact.telecom`. It is a real address, so it is a real hit, and it is not PHI:
# it denotes a standards-body mailing list, not a person receiving care.
#
# It is the only address anywhere under `test/us-core-profiles/`, and it appears in
# 10 of the 10 files there
#   (grep -rhoE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' test/us-core-profiles/ | sort -u)
# measured 2026-08-02. Declared as an exact EMAIL rather than
# `EMAILDOMAIN lists.hl7.org`, which would clear every address at a live
# organizational domain, and rather than an `--allow-fixture` bypass, which would
# silence every other check on those 10 files AND would need a matching override-log
# entry that the bare `pnpm phi-scan` in CI and the pre-commit hook never pass.
# Narrowest declaration that covers the finding.
EMAIL cgp@lists.HL7.org

# The publisher contact in `package.json`'s `author` field. It became visible the day
# all-mode stopped stopping at `src/`, `test/` and `scripts/` and started reconciling
# against everything git tracks: `package.json` had never been opened by either route.
#
# It is NAMED here rather than removed, because removing it would delete the evidence
# that the sweep happened and would strip a real field off the published manifest: it is
# this company's own published contact address, already on the npm registry page for
# every `@cosyte/*` package, and it denotes an organization rather than a person
# receiving care. It is the ONLY email anywhere in the 22 files that widening newly
# opened (measured 2026-08-08 over the tracked corpus outside the scan roots, markdown
# and the vendored archives aside), and there is no SSN shape in any of them.
#
# Declared as an exact EMAIL rather than `EMAILDOMAIN cosyte.com`, which would clear
# every address at a live organizational domain, including one a future fixture might
# carry by mistake. Narrowest declaration the allow-list has.
#
# ▶ AND SAY WHAT IT COSTS, BECAUSE A REFUTER CAUGHT THE FIRST VERSION OF THIS SLICE
# CLAIMING IT COST NOTHING. AN ALLOW-LIST ENTRY IS GLOBAL AND ROUTE-BLIND. It is read
# once and consumed inside `scanTarget`, so this line clears that one literal address in
# EVERY mode, `--staged` and `--allow-fixture`-free `paths` mode included, not just in
# the all-mode sweep that newly opened `package.json`. A file under `src/` carrying
# `hello@cosyte.com` reds on the base of this change and does not red now. That is a real
# subtraction on the commit-blocking route, it is one literal value rather than a rule,
# and it is written here rather than left for the next reader to discover.
#
# IT IS ACCEPTED, NOT OVERLOOKED, AND THE ALTERNATIVES ARE WORSE. There is no
# path-scoped value declaration in this scanner, and inventing one would put a second
# moving part inside a PHI detector to spare one organizational address. The other
# mechanism, `--allow-fixture package.json` plus an override-log entry, is not reachable
# at all here: CI runs a bare `pnpm phi-scan` and the pre-commit hook runs
# `pnpm phi-scan --staged`, neither of which passes the flag, so the hit would simply
# stand. This is the same shape and the same trade as the `cgp@lists.HL7.org` entry
# above, which the base already applied on every route.
#
# BOTH DIRECTIONS ARE PINNED by `test/scripts/phi-scan.test.ts`: that this address is
# cleared, and that ANY OTHER address at the same domain still reds. The second is what
# stops this line being quietly widened into `EMAILDOMAIN cosyte.com` later.
EMAIL hello@cosyte.com

# --- Synthetic person-name tokens (example: extend for your fixtures) ---
# NAME DOE
# NAME JOHN
#
# The shipped clearly-fake name pool (src/safe/names-pool.ts). Every PID-5 name token in a generated
# HL7 fixture must appear here: the structured HL7 detector in scripts/phi-scan.ts flags any name
# token not positively declared synthetic. Keep this in sync with the pool.
NAME TESTINA
NAME FIXTURA
NAME SYNTHOS
NAME PLACEHOLDA
NAME SAMPLETON
NAME PROTIUS
NAME PROTOTIUS
NAME STUBBINA
NAME EXAMPLA
NAME QUILLIAM
NAME FABRICA
NAME SIMULA
NAME TESTRY
NAME SEEDWIN
NAME CORPORA
NAME REPRODO
NAME MOCKTAVIA
NAME DUMMETT
NAME VOIDWIN
NAME DETERIA
NAME RANDOMIR
NAME TESTERSON
NAME FAUXMAN
NAME PLACEHOLT
NAME MOCKRIDGE
NAME FIXTINGHAM
NAME SYNTHWELL
NAME DUMMERTON
NAME EXAMPLEWOOD
NAME FABRICANT
NAME SIMULACRE
NAME NONESUCH
NAME SEEDMAN
NAME CORPUSWORTH
NAME REPRODUS
NAME VOIDMARK
NAME DETERWOOD
NAME RANDOMSON
NAME QUILLFEATHER
NAME NOTREAL
NAME GENFIELD

# --- Hand-written test tokens, NOT part of the generator's fake-name pool ---
#
# The two below are NOT in `src/safe/names-pool.ts` and must never be added to it.
# They are hand-written literals inside `test/deid/loop.test.ts`, which asserts what
# `recordTargetSentinels` DROPS, and they became visible the day the C-CDA arm stopped
# gating on the `.xml` file extension and started reading documents written inline in
# TypeScript. They were always in the repo; nothing could see them.
#
# Each is here rather than edited out of the fixture because the fixture needs exactly
# these shapes to prove its point, and rewriting a de-identifier test to quiet a PHI
# scanner would couple two gates and change what the test proves:
#   AL        a 2-character given name, present to prove the sentinel extractor drops
#             non-distinctive tokens. Any replacement must also be 2 characters, so no
#             pool name can stand in.
#   PROVIDER  a role word in an <author> name, present to prove author names are out of
#             the recordTarget sentinel scope.
#
# WHAT THIS COSTS, stated so a reviewer prices it rather than skims it: a NAME entry is
# global, so a `<given>Al</given>` or a PID-5 `Provider` now passes ANYWHERE in the repo.
# `Provider` is a role word no person is named, so that is close to free. `Al` is a real
# nickname, and declaring it is a genuine if small weakening: accepted because a
# 2-character token carries almost no identifying information, which is the same judgment
# the de-identifier under test already encodes by dropping it.
NAME AL
NAME PROVIDER

# --- Synthetic dates of birth (example: normalize to your detector's format) ---
# DOB 19800115

# --- Synthetic ids: SSN / MRN / member-id shapes (example) ---
# ID MRN12345
